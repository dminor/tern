# A nonsensical example of what's supported in the language.
next(solve(conj {
    'apple == 'apple,
    var (x) {x == 'apple},
    disj {
        var (y) {
            y == 'orange
        } |
        'apple == 'orange
    }
}, {}))
